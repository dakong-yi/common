syntax = "proto3";

option go_package = "./pkg/pb/error";

package error;

message Error {
  int32 code = 1;
  string message = 2;
}

enum ErrorCode {
  UNKNOWN = 0;
  INVALID_ARGUMENT = 1;
  NOT_FOUND = 2;
}